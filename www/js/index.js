function blurMain() {
$('.app').css({'opacity': 0.3, 'pointer-events': 'auto'})
//rest of function
}

function unblurMain() {
$('.app').css({'opacity': 1, 'pointer-events': 'auto'})
//rest of function
}